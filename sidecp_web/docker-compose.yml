version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sidecp-web
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
